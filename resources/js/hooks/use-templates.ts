"use client"

// Simple hook for templates without API calls
export function useTemplates() {
  // This hook is now simplified since we removed API dependencies
  // All template management is handled directly in the component
  return {
    // Placeholder for future use
  }
}
